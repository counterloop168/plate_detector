# 🏗️ System Architecture

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         Web Browser                             │
│                    (User Interface)                             │
└─────────────────────┬───────────────────────────────────────────┘
                      │
                      │ HTTP Requests
                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Flask Web Server (app.py)                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Routes & API Endpoints                                    │ │
│  │  - /                    (Dashboard)                        │ │
│  │  - /camera/1, /camera/2 (Live Streams)                     │ │
│  │  - /test                (API Test Page)                    │ │
│  │  - /api/process-video   (Video Batch Processing)           │ │
│  │  - /api/recent-detections (Statistics)                     │ │
│  │  - /camera/start        (Start Camera)                     │ │
│  │  - /camera/stop         (Stop Camera)                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Error Handling & Validation                               │ │
│  │  - Input validation (camera_id, confidence)                │ │
│  │  - Error recovery & logging                                │ │
│  │  - Startup checks integration                              │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────┬───────────────────────┬───────────────────────┘
                  │                       │
        ┌─────────▼─────────┐   ┌─────────▼──────────┐
        │  plate_detection  │   │ plate_deduplication│
        │      .py          │   │       .py          │
        └─────────┬─────────┘   └──────────┬─────────┘
                  │                        │
        ┌─────────▼────────────────────────▼───────────┐
        │         plate_persistence.py                 │
        └─────────┬────────────────────────────────────┘
                  │
    ┌─────────────┼─────────────┬──────────────┐
    │             │             │              │
    ▼             ▼             ▼              ▼
┌────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ YOLO   │  │ SQLite   │  │  Google  │  │  Google  │
│ Model  │  │ Database │  │  Drive   │  │  Sheets  │
│best.pt │  │(site.db) │  │          │  │          │
└────────┘  └──────────┘  └──────────┘  └──────────┘
            (AUTO-SETUP)

┌──────────────────────────────────────────────────────────────┐
│  Support Infrastructure                                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │health_check  │  │init_database │  │error_handlers│        │
│  │    .py       │  │    .py       │  │    .py       │        │
│  │(AUTO-INSTALL)│  │(AUTO-SCHEMA) │  │(VALIDATION)  │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │startup_checks│  │camera_manager│  │  constants   │        │
│  │    .py       │  │    .py       │  │    .py       │        │
│  │(PRE-FLIGHT)  │  │(SINGLETON)   │  │(SHARED VARS) │        │
│  └──────────────┘  └──────────────┘  └──────────────┘        │
└──────────────────────────────────────────────────────────────┘
```

## Component Details

### 1. Frontend Layer

#### Web Browser
```
┌─────────────────────────────────┐
│  templates/index.html           │
│  ┌───────────────────────────┐  │
│  │ - Live Camera Feed        │  │
│  │ - Detection Results       │  │
│  │ - Statistics Dashboard    │  │
│  │ - Recent Detections       │  │
│  │ - System Status           │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
         │
         │ AJAX/Fetch API
         ▼
┌─────────────────────────────────┐
│  templates/camera.html          │
│  ┌───────────────────────────┐  │
│  │ - Camera Stream Control   │  │
│  │ - Video Stream Display    │  │
│  │ - Detection Monitoring    │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
         │
         │ AJAX/Fetch API
         ▼
┌─────────────────────────────────┐
│  templates/test.html            │
│  ┌───────────────────────────┐  │
│  │ - API Endpoint Tests      │  │
│  │ - JSON Response Viewer    │  │
│  │ - Quick Testing Interface │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### 2. Backend Layer

#### Flask Application (app.py)
```
┌─────────────────────────────────────────────┐
│  Flask App                                  │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Startup Integration                   │ │
│  │  - Run startup_checks()                │ │
│  │  - Initialize database                 │ │
│  │  - Validate configuration              │ │
│  │  - Load models with error recovery     │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Request Handler                       │ │
│  │  - File upload validation              │ │
│  │  - Input sanitization                  │ │
│  │  - Type checking                       │ │
│  │  - Request parsing                     │ │
│  │  - Response formatting                 │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  API Endpoints                         │ │
│  │  - Camera management                   │ │
│  │  - Configuration                       │ │
│  │  - Detection                           │ │
│  │  - Statistics                          │ │
│  │  - Cache Management                    │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Error Handling                        │ │
│  │  - Specific exceptions                 │ │
│  │  - Lazy logging                        │ │
│  │  - Return value checks                 │ │
│  │  - Graceful degradation                │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Health Check Module
```
┌─────────────────────────────────────────────┐
│  health_check.py                            │
│  ┌────────────────────────────────────────┐ │
│  │  Package Verification                  │ │
│  │  - Check 9 required packages           │ │
│  │  - Check 4 optional packages           │ │
│  │  - Auto-install missing                │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  System Checks                         │ │
│  │  - Files & directories                 │ │
│  │  - Configuration validity              │ │
│  │  - Database schema                     │ │
│  │  - GPU/CUDA availability               │ │
│  │  - Disk space monitoring               │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Database Initialization
```
┌─────────────────────────────────────────────┐
│  init_database.py                           │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Schema Management                     │ │
│  │  - Auto-create tables                  │ │
│  │  - Create performance indexes          │ │
│  │  - Verify schema integrity             │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Error Handlers Module
```
┌─────────────────────────────────────────────┐
│  utils/error_handlers.py                    │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Custom Exceptions                     │ │
│  │  - ValidationError                     │ │
│  │  - ConfigurationError                  │ │
│  │  - ModelError                          │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Validators                            │ │
│  │  - validate_camera_id()                │ │
│  │  - validate_confidence()               │ │
│  │  - validate_plate_number()             │ │
│  │  - validate_bbox()                     │ │
│  │  - validate_config()                   │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Utilities                             │ │
│  │  - load_json_safe()                    │ │
│  │  - save_json_safe()                    │ │
│  │  - check_disk_space()                  │ │
│  │  - sanitize_filename()                 │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Startup Checks Module
```
┌─────────────────────────────────────────────┐
│  utils/startup_checks.py                    │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Pre-flight Validation                 │ │
│  │  - Python version check                │ │
│  │  - Dependencies check                  │ │
│  │  - Model files check                   │ │
│  │  - Config validation                   │ │
│  │  - Directory creation                  │ │
│  │  - Database initialization             │ │
│  │  - Port availability                   │ │
│  │  - CUDA/GPU detection                  │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 3. Processing Layer

#### Plate Detection Module
```
┌─────────────────────────────────────────────┐
│  utils/plate_detection.py                   │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Model Management                      │ │
│  │  - YOLO Detector Loading               │ │
│  │  - Model validation                    │ │
│  │  - ALPR Initialization                 │ │
│  │  - GPU/CPU Selection                   │ │
│  │  - Error recovery                      │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Detection Pipeline                    │ │
│  │  - Image validation                    │ │
│  │  - YOLO inference                      │ │
│  │  - Confidence threshold checks         │ │
│  │  - Bounding box validation             │ │
│  │  - Result verification                 │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  OCR Processing                        │ │
│  │  - fast-alpr integration               │ │
│  │  - Character recognition               │ │
│  │  - Result filtering                    │ │
│  │  - Error handling                      │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Post-processing                       │ │
│  │  - Region extraction                   │ │
│  │  - Plate number validation             │ │
│  │  - Coordinate normalization            │ │
│  │  - Result formatting                   │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Camera Manager Module
```
┌─────────────────────────────────────────────┐
│  utils/camera_manager.py                    │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Stream Management                     │ │
│  │  - Camera ID validation                │ │
│  │  - Connection state tracking           │ │
│  │  - Error recovery                      │ │
│  │  - Resource cleanup                    │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Frame Processing                      │ │
│  │  - Frame capture validation            │ │
│  │  - Encoding checks                     │ │
│  │  - Buffer management                   │ │
│  │  - Performance optimization            │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Detection Integration                 │ │
│  │  - Real-time plate detection           │ │
│  │  - Result caching                      │ │
│  │  - Thread safety                       │ │
│  │  - Region-based detection              │ │
│  │  - Text history tracking               │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Deduplication Module
```
┌─────────────────────────────────────────────┐
│  utils/plate_deduplication.py               │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Hash-based Detection                  │ │
│  │  - Perceptual hashing                  │ │
│  │  - Input validation                    │ │
│  │  - Time-window check                   │ │
│  │  - Similarity calculation              │ │
│  │  - Threshold validation                │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Spatial Analysis                      │ │
│  │  - Bounding box comparison             │ │
│  │  - IOU calculation with checks         │ │
│  │  - Overlap threshold validation        │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Cache Management                      │ │
│  │  - Recent plates cache                 │ │
│  │  - Last saved tracking                 │ │
│  │  - Memory management                   │ │
│  │  - Automatic cleanup                   │ │
│  │  - Statistics (detection counts)       │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

#### Persistence Module
```
┌─────────────────────────────────────────────┐
│  utils/plate_persistence.py                 │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Local Storage                         │ │
│  │  - Image saving with validation        │ │
│  │  - Directory management                │ │
│  │  - File organization                   │ │
│  │  - Encoding checks                     │ │
│  │  - Disk space monitoring               │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Google Drive Integration              │ │
│  │  - Authentication with error recovery  │ │
│  │  - Folder creation                     │ │
│  │  - File upload with retry              │ │
│  │  - Link generation                     │ │
│  │  - Error handling                      │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Google Sheets Integration             │ │
│  │  - Spreadsheet access                  │ │
│  │  - Sheet creation                      │ │
│  │  - Row appending with validation       │ │
│  │  - Data formatting                     │ │
│  │  - Error handling                      │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Cleanup Operations                    │ │
│  │  - Old file removal with safety        │ │
│  │  - Drive cleanup                       │ │
│  │  - Sheet cleanup                       │ │
│  │  - Error handling                      │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 4. Storage Layer

```
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Local Storage  │  │  Google Drive   │  │ Google Sheets   │
├─────────────────┤  ├─────────────────┤  ├─────────────────┤
│ captures/       │  │ Plate_Detections│  │ Spreadsheet     │
│ └─ YYYY-MM-DD/  │  │ └─ YYYY-MM-DD/  │  │ └─ YYYY-MM-DD   │
│    └─ camera_X/ │  │    └─ IN/       │  │    (sheets)     │
│       └─ *.jpg  │  │    └─ OUT/      │  │                 │
│                 │  │       └─ *.jpg  │  │ Columns:        │
│ uploads/        │  │                 │  │ - Timestamp     │
│ └─ temp files   │  │ Retention:      │  │ - Camera        │
│                 │  │ - 7 days        │  │ - Plate Number  │
│ instance/       │  │ (AUTO-CLEANUP)  │  │ - Confidence    │
│ └─ site.db      │  │                 │  │ - Image Link    │
│ (AUTO-CREATED)  │  │                 │  │                 │
│                 │  │ [OPTIONAL]      │  │ [OPTIONAL]      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### 5. Model Layer

```
┌─────────────────────────────────────────────┐
│  utils/models/                              │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  best.pt (Primary YOLO Model)          │ │
│  │  - Custom trained YOLO 11              │ │
│  │  - Vietnamese plate detection          │ │
│  │  - Requires Ultralytics 8.3.218+       │ │
│  │  - GPU/CPU compatible                  │ │
│  │  - ~50-200MB                           │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  fast-alpr OCR Model                   │ │
│  │  - cct-s-v1-global-model               │ │
│  │  - Text recognition                    │ │
│  │  - Downloaded automatically            │ │
│  │  - Requires onnxruntime                │ │
│  └────────────────────────────────────────┘ │
│                                             │
│  ┌────────────────────────────────────────┐ │
│  │  Model Validation                      │ │
│  │  - File existence checks               │ │
│  │  - Version compatibility               │ │
│  │  - GPU/CUDA availability               │ │
│  │  - Error recovery to CPU               │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

## Data Flow

### Startup Flow
```
1. User runs start_server.bat
   │
   ▼
2. Python health_check.py
   │
   ├─▶ Check required packages (9)
   │   └─▶ Auto-install missing packages
   │
   ├─▶ Check optional packages (4)
   │   └─▶ Warn if missing (non-blocking)
   │
   ├─▶ Verify system files
   │
   ├─▶ Validate configuration
   │
   ├─▶ Check database schema
   │
   ├─▶ Detect GPU/CUDA
   │
   └─▶ Monitor disk space
   │
   ▼
3. Python app.py (if health check passes)
   │
   ▼
4. startup_checks()
   │
   ├─▶ Check Python version (≥3.8)
   │
   ├─▶ Verify dependencies
   │
   ├─▶ Validate model files
   │
   ├─▶ Load & validate config
   │
   ├─▶ Create required directories
   │
   ├─▶ Initialize database (auto-create schema)
   │
   ├─▶ Check port availability (5000)
   │
   └─▶ Detect CUDA/GPU
   │
   ▼
5. Load ML models with error recovery
   │
   ▼
6. Flask server starts on http://127.0.0.1:5000
```

### Real-Time Camera Detection Flow
```
1. User starts camera
   │
   ▼
2. Flask receives POST /api/camera/start
   │
   ├─▶ Validate camera_id
   │
   └─▶ Check camera availability
   │
   ▼
3. Camera stream initialized
   │
   ├─▶ Verify camera connection
   │
   └─▶ Handle connection errors
   │
   ▼
4. Frame capture loop begins
   │
   ├─▶ Validate frame capture
   │
   └─▶ Check encoding success
   │
   ▼
5. plate_detection.detect_plates(frame, camera_id)
   │
   ├─▶ Validate input image
   │
   ├─▶ Region extraction
   │
   ├─▶ YOLO detection
   │   └─▶ Confidence threshold check
   │
   ├─▶ Bounding box validation
   │
   ├─▶ OCR recognition (fast-alpr)
   │   └─▶ Error handling
   │
   ├─▶ Plate number validation
   │
   └─▶ Tracking update
   │
   ▼
6. plate_deduplication.is_duplicate()
   │
   ├─▶ Time window check
   │
   ├─▶ Hash comparison
   │
   └─▶ Duplicate decision
   │
   ▼
7. plate_persistence.persist_plate_detection()
   │
   ├─▶ Save local image
   │
   ├─▶ Upload to Drive
   │
   ├─▶ Append to Sheets
   │
   └─▶ Save to database
   │
   ▼
8. Stream frame to browser (MJPEG)
   │
   └─▶ Continuous loop until camera stopped
```

### Recent Detections Flow
```
1. User requests recent detections
   │
   ▼
2. Flask receives GET /api/recent-detections
   │
   ▼
3. plate_deduplication.get_recent_plates(camera_id, minutes)
   │
   ├─▶ Filter by camera (optional)
   │
   ├─▶ Filter by time window
   │
   └─▶ Sort by timestamp
   │
   ▼
4. Return JSON array of detections
```

## Configuration Flow

```
┌───────────────────────────────────
│  Configuration Files                         
│                                              
│  plate_detection_config.json                
│  ├─▶ enabled_cameras                     
│  ├─▶ detection_fps                          
│  ├─▶ confidence_threshold                   
│  ├─▶ deduplication settings                 
│  └─▶ storage settings                       
│                                             
│  plate_detection_credentials.json           
│  ├─▶ Google Drive credentials               
│  └─▶ Google Sheets credentials              
│                                              
│  .env                                       
│  ├─▶ HOST, PORT                             
│  ├─▶ DEBUG mode                             
│  └─▶ SECRET_KEY                             
└────────────────────────────────────
         │
         ▼
┌──────────────────────────────────────────────┐
│  Application Modules                         │
│                                              │
│  Load at startup:                            │
│  1. plate_detection.load_config()            │
│  2. plate_persistence.load_config()          │
│  3. plate_deduplication reads config         │
│  4. Flask app reads .env                     │
└──────────────────────────────────────────────┘
```

## Deployment Architecture

### Development
```
┌─────────────┐
│ Developer   │
│  Machine    │
├─────────────┤
│ Windows     │
│ Python 3.9  │
│ Port 5000   │
│ Debug: True │
└─────────────┘
```

### Production (Option 1: Direct)
```
┌────────────────┐
│ Server         │
├────────────────┤
│ Linux/Windows  │
│ Python 3.9     │
│ Port 80/443    │
│ HTTPS          │
│ Supervisor     │
└────────────────┘
```

### Production (Option 2: Docker)
```
┌────────────────────────────────┐
│ Docker Host                    │
│ ┌────────────────────────────┐ │
│ │ plate-detector Container   │ │
│ │ ┌────────────────────────┐ │ │
│ │ │ Flask App              │ │ │
│ │ │ Port 5000 → 80         │ │ │
│ │ └────────────────────────┘ │ │
│ │                            │ │
│ │ Volumes:                   │ │
│ │ - ./captures               │ │
│ │ - ./uploads                │ │
│ │ - ./models                 │ │
│ └────────────────────────────┘ │
└────────────────────────────────┘
```

## Integration Points

```
External System          API Endpoint
─────────────────       ──────────────────
Camera System      ─▶   POST /api/camera/start
                   ─▶   POST /api/camera/stop
                   ─▶   GET  /api/camera/stream/1
Monitoring Tool    ─▶   GET  /api/health
Dashboard          ─▶   GET  /api/recent-detections
Admin Tool         ─▶   POST /api/clear-cache
Backup System      ─▶   POST /api/cleanup
Analytics Tool     ─▶   GET  /api/tracking-stats
```

---

**This architecture supports:**
- ✅ Scalability
- ✅ Maintainability
- ✅ Easy integration
- ✅ Production deployment
- ✅ Real-time processing
